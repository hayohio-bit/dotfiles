# Scoop 설치 목록 (관리자 권한 불필요)
#
# - 한 줄에 패키지 하나. '#' 로 시작하는 줄과 빈 줄은 무시된다.
# - '#>' 로 시작하는 줄은 그룹 이름이다. -Select 선택 화면에서 구분선으로 표시된다.
# - main 버킷 외 버킷이 필요한 패키지는 'bucket/package' 형식으로 적는다.
#   bootstrap.ps1 이 접두사를 읽어 필요한 버킷을 자동으로 추가한다.
# - 여기 있는 개발 툴체인은 winget 목록과 중복되지 않도록 winget-apps.json 에서 제외되어 있다.

#> 런타임
git
nodejs-lts
python
java/temurin21-jdk

#> 버전 관리자
# 프로젝트별 버전 요구가 생겼을 때 즉시 스위칭하기 위해 함께 설치한다.
# nodejs-lts / python 과 shim 이 겹치므로 나중에 설치된 쪽이 PATH 우선권을 가진다.
# 버전 전환이 필요해지면 아래를 실행한다.
#   nvm install lts   / nvm use lts
#   pyenv install 3.13.0 / pyenv global 3.13.0
nvm
pyenv

#> CLI 도구
gh
7zip
sudo
ripgrep
fzf
